export default function SettingsPage() {
  return (
    <section>
      <h1 className="hero-title" style={{ fontSize: "1.5rem" }}>
        Settings
      </h1>
      <p style={{ color: "var(--ads-text-muted)", marginTop: "0.5rem" }}>Profile and site preferences (expand later).</p>
    </section>
  );
}
