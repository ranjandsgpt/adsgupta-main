/**
 * Vercel Postgres — use `sql` tagged template in route handlers / server code.
 * @see https://vercel.com/docs/storage/vercel-postgres
 */
import { sql } from "@vercel/postgres";

export { sql };
