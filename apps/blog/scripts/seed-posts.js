/**
 * Legacy SQLite seed removed — use Vercel Postgres + GET /api/db-init (with DB_INIT_SECRET),
 * then create posts via the admin UI or SQL.
 */
console.error(
  "scripts/seed-posts.js is disabled. Use Vercel Postgres and the CMS admin, or insert rows manually."
);
process.exit(1);
