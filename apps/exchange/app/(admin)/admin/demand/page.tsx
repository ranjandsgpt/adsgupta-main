export default function AdminDemandPartnersPage() {
  return (
    <div>
      <h1 style={{ color: "var(--text-bright)", marginTop: 0 }}>Demand partners</h1>
      <p style={{ color: "var(--text-muted)", fontSize: 12 }}>
        Wrapper and DSP catalog (static UI shell). Direct OpenRTB demand uses the auction engine + seat
        tools in the Demand portal.
      </p>
    </div>
  );
}
