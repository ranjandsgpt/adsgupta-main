export const dynamic = "force-dynamic";

export default function PublisherReportingPage() {
  return (
    <div>
      <h1 style={{ color: "var(--text-bright)", marginTop: 0 }}>Reporting</h1>
      <p style={{ color: "var(--text-muted)", fontSize: 12 }}>
        Same aggregates as the dashboard card, with optional CSV/export in a future pass.
      </p>
    </div>
  );
}
