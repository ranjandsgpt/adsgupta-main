export const dynamic = "force-dynamic";

export default function PublisherTagsPage() {
  return (
    <div>
      <h1 style={{ color: "var(--text-bright)", marginTop: 0 }}>Tags</h1>
      <p style={{ color: "var(--text-muted)", fontSize: 12 }}>
        Use <code>mde.js</code> from this site with <code>defineSlot</code>{" "}
        <code>adUnitId</code> set to your UUID from inventory.
      </p>
    </div>
  );
}
