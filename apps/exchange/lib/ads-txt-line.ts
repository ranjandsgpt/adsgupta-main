/** Single ads.txt line for a publisher (TAG-ID = mde-exchange-001). */
export function formatPublisherAdsTxtLine(publisherId: string): string {
  return `exchange.adsgupta.com, ${publisherId}, DIRECT, mde-exchange-001`;
}
