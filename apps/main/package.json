{
  "name": "main",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm install && npm run start",
    "build": "cd frontend && npm install && npm run build",
    "lint": "cd frontend && npm install && npm run test"
  }
}

