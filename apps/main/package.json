{
  "name": "main",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run start",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run test"
  }
}

